# development environment
# based on `docker-compose.yml`

version: '3.2'

services:
  wp:
    volumes:
      - type: bind
        source: ./wp/code
        target: /var/www/html
    environment:
      WORDPRESS_DEBUG: 1
